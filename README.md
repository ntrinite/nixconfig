# README

NixOS Config

TODO List:
Some of these probably relate to each other and can be solved in one task but I at least want the general idea written down

- [ ] Actually make hosts a little more extensible
  - Basically utilize `config` more and create common files that just use the config (users, home manager, etc)
  - I don't really care about other users
- [ ] Create "common" config that all hosts can import to get default packages and settings
- [ ] update to Flake parts
- [ ] Support for non-NixOS systen